{% extends 'layout.html.twig' %}

{% block content %}
    {{ dump(issue) }}
    {{ issue.creationDate|age }}
    {% for comment in comments %}
        {{ dump(comment) }}
        {{ comment.creationDate|age }}
    {% endfor %}
{% endblock %}